<center>

<h1>Burger Town!</h1>
<h2>Look at all these great Burgers!</h2>

</center>

<div class="container">
    <div class="row">
        <div class="col-xs-6">
            <ul class="uneaten">
                <p>EAT THESE BURGERS!</p>
                {{#each burgers}}
                {{#unless devoured}}
                <li>
                    <span> {{this.burger_name}} </span>
                <button class="change-devour" data-id="{{id}}" data-newDevour="{{devoured}}"> DEVOUR
                </button>    </li>
                {{/unless}}
            {{/each}}
            </ul>
        </div>
</div>

<div class="container">
        <div class="col-xs-6">
            <ul class="devoured">
                <p>DEVOURED BURGERS</p>
                <hr>
                {{#each burgers}}
                    {{#if devoured}}
                    <li> {{this.id}}
                        {{this.burger_name}}</li>
                    {{/if}}
                {{/each}}
            </ul>
        </div>
</div>


<center>
    <div class="form">
<h2>Add a Burger!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burg">Burger Name:</label>
    <input type="text" id="burg" name="burger_name">
    <button type="submit">Add Burger</button>
    </div>
</form>
</div>
</center>